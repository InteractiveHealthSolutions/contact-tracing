<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ihs.android.contracttracing.views.activities">

    <!-- To auto-complete the email text field in the login fragment_contact_form with the user's emails -->
    >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.ihs.android.contracttracing.contracttracing.views.activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait"
            ></activity>
        <activity
            android:name="com.ihs.android.contracttracing.contracttracing.views.activities.LoginActivity"
            android:label="Contact Tracing"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.ihs.android.contracttracing.contracttracing.views.activities.ContactTraceActivity"
            android:label="@string/search_contact"
            android:screenOrientation="portrait"
            />
        <activity android:name="com.ihs.android.contracttracing.contracttracing.views.activities.ContactFormActivity"
            android:label="@string/contact_form"
            android:screenOrientation="portrait"
            />
        <activity android:name="com.ihs.android.contracttracing.contracttracing.views.activities.OfflineFormActivity"
            android:label="@string/offline_form"
            android:screenOrientation="portrait"
            ></activity>
    </application>

</manifest>